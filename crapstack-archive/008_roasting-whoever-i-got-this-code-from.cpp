#include <iostream>
int main()
{
    int wiek;
    std::cin >> wiek;
    if( wiek >= 18 )
    if( wiek <= 100 )
    {
        std::cout << "text redacted bc it was cringe #1" << std::endl;
        std::cout << "(I know because of the 2020 comment right next to this line, which implies I copied this code from somewhere lol)" << std::endl; // jesus christ polish people are so fuckign cringe
    }
    std::cout << "Koniec" << std::endl;
    return 0;
}
